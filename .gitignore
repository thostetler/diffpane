node_modules/
dist/
*.tgz
# Cloned fixture repos and build artefacts; ~700MB. The spike's sources are
# tracked — they are the port's reference implementation.
spike/target/
spike/repos/
rust/target/
